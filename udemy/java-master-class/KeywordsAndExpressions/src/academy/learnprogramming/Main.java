package academy.learnprogramming;

public class Main {

    public static void main(String[] args) {
        // the expression is everything except the data type and the semicolon
        double kilometers = (100 * 1.609344);
        int highScore = 50;

        // the expression is everything inside parenthesis
        if (highScore == 50) {
            // everything in the parenthesis is an expression as well
            System.out.println("This is an expression");
        }


    }
}
